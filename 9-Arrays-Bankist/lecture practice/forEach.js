const movements = [200, 450, -400, 3000, -650, -130, 70, 1300];


movements.forEach(function(movement,i,arr)
{
    console.log(i,arr,movement);
});

//it calls the callback function on each element of the array and first parameter 
//represents the value at each index in array

